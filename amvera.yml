version: null
meta:
  environment: node
  toolchain:
    name: npm
    version: 18
    
build:
  commands:
    - cd client
    - npm install
    - npm run build

start:
  command: echo "build only"
    
